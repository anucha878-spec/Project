# qa-speckit-workflow (V2, skill-based)

QA Spec-Kit Execution Workflow — 15 owned QA skills + 3 community helper skills + central
HTML execution-report template. Self-contained: bundled `qa-shared/` is provisioned into
`<project>/qa-shared/` (copy-if-missing) so relative refs resolve on any machine.
Reads/writes QA artifacts under `spec-kit/<feature>/qa/`.

ดูรายละเอียด pipeline + วิธีติดตั้งที่ README ของ marketplace (root repo).
