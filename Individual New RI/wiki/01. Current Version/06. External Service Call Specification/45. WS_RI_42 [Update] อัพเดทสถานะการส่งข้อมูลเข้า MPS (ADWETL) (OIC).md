# WS_RI_42 [Update] อัพเดทสถานะการส่งข้อมูลเข้า MPS (ADWETL) (OIC)

> **Source:** http://wiki.thaisamut.co.th/pages/viewpage.action?pageId=1279721887  
> **Page ID:** 1279721887  
> **Path:** Home / Current Version / 06. External Service Call Specification / WS_RI_42 [Update] อัพเดทสถานะการส่งข้อมูลเข้า MPS (ADWETL) (OIC)

---

[
Overview
] [
Input
] [
Process
] [
Output
] [
Exception
] [
Example Input & Output
]

## Overview

สำหรับ Update สถานะและ count จำนวนรายการที่ระบบ MPS เมื่อส่งข้อมูลลสำเร็จหรือไม่สำเร็จตามรอบประมวลผล

**Repositories**: msa-adwetl

**Service path**

**PUT**:****

****

Icon
TYPE : <PUT>

**อธิบายได้ดังนี้**

GET - Select

POST - Insert

PUT - Update

DELETE - Delete

## Input

<แสดงข้อมูล Paameter ที่ระบบนี้จะต้องส่งไปยัง external service>

| Name | Type | Require | Description | Example | Validation | Remark |
| --- | --- | --- | --- | --- | --- | --- |
| ri_est_hd_id | numeric | Require | PK [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd) | 1 |  |  |

## Process

ดำเนินการ Update ข้อมูล ดังนี้

1. กรณีส่งข้อมูลเข้าระบบ edw ครบแล้วทุกรายการตามรอบประมวลผลให้ update mps process ดังนี้กรณี responseCode = 000 (SUCCESS) ทุกรายการ if [tx_ri_process_header_oic](http://wiki.thaisamut.co.th/display/RDSADW/tx_ri_process_header_oic).round = 1ให้ดำเนินการ update ข้อมูลต่อไปนี้ที่ [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header) where cf_file_name_id = 87 and [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header).period = [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd).period    msa-adwetl  No.NameTypeCondition1ms_import_status_idnumeric 72sum_amountnumericcount(ri_process_hd_id) ที่ success ตามรอบประมวลผล where coi_flag is false3updated_byvarcharuser login4updated_datedateTimestamp ที่ update5remarkvarcharNULL6coi_flagbooleanfalse7transaction_datedateTimestamp ที่ update  if [tx_ri_process_header_oic](http://wiki.thaisamut.co.th/display/RDSADW/tx_ri_process_header_oic).round = 2ให้ดำเนินการ update ข้อมูลต่อไปนี้ที่ [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header) where cf_file_name_id = 88 and [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header).period = [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd).period    msa-adwetl  No.NameTypeCondition1ms_import_status_idnumeric 72sum_amountnumericcount(ri_process_hd_id) ที่ success ตามรอบประมวลผล where coi_flag is true 3updated_byvarcharuser login4updated_datedateTimestamp ที่ update5remarkvarcharNULL6coi_flagbooleantrue7transaction_datedateTimestamp ที่ update  กรณีพบ responseCode = 100 (Error) บางรายการ if [tx_ri_process_header_oic](http://wiki.thaisamut.co.th/display/RDSADW/tx_ri_process_header_oic).round = 1ให้ดำเนินการ update ข้อมูลต่อไปนี้ที่ [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header) where cf_file_name_id = 87  and [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header).period = [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd).period    msa-adwetl  No.NameTypeCondition1ms_import_status_idnumeric32sum_amountnumericcount(ri_process_hd_id) ที่ success ตามรอบประมวลผล where coi_flag is false3updated_byvarcharuser login4updated_datedateTimestamp ที่ update5remarkvarcharนำเข้าข้อมูลไม่สำเร็จ รบกวนติดต่อผู้ดูแลระบบ6coi_flagbooleanfalse7transaction_datedateTimestamp ที่ update  if [tx_ri_process_header_oic](http://wiki.thaisamut.co.th/display/RDSADW/tx_ri_process_header_oic).round = 2ให้ดำเนินการ update ข้อมูลต่อไปนี้ที่ [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header) where cf_file_name_id = 88 and [tx_mps_base_header](http://wiki.thaisamut.co.th/display/RDSADW/tx_mps_base_header).period = [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd).period    msa-adwetl  No.NameTypeCondition1ms_import_status_idnumeric 32sum_amountnumericcount(ri_process_hd_id) ที่ success ตามรอบประมวลผล where coi_flag is true 3updated_byvarcharuser login4updated_datedateTimestamp ที่ update5remarkvarcharนำเข้าข้อมูลไม่สำเร็จ รบกวนติดต่อผู้ดูแลระบบ6coi_flagbooleantrue7transaction_datedateTimestamp ที่ update  เมื่อ responseCode = 100 (Error) จะมีการเรียก [WS_RI_41](http://wiki.thaisamut.co.th/pages/viewpage.action?pageId=1266090128) เพื่อ update ข้อมูลกลับไปที่ [tx_ri_est_hd](http://wiki.thaisamut.co.th/display/RDSINRI/13.+tx_ri_est_hd).mps_status = 100กรณีมีการกดส่งข้อมูลมาใหม่ (ส่งซ้ำรายการเดิม) เมื่อตรวจสอบแล้วว่าส่งสำเร็จทั้งหมดทุกรายการให้กลับไป update ตามข้อ a. กรณี responseCode = 000 (SUCCESS)

## Output

<แสดงข้อมูลที่ได้รับจาก external service นี้>

| Name | Type | Require | Description | Example | Validation | Remark |
| --- | --- | --- | --- | --- | --- | --- |
| responseCode | numeric | Require | Code ผลการ update ข้อมูล | 000 |  | if SUCCESS = 000if ERROR = 100 |
| mps_import_status_id | numeric | Require | id สถานะรายการ | 7 |  |  |
| sum_amount | numeric | Require |  | 10 |  |  |
| updated_by | varchar | Require |  | RI |  |  |
| updated_date | date | Require | Timestamp ที่ update |  |  |  |
| remark | varchar |  | หมายเหตุ |  |  |  |
| coi_flag | boolean | Require | flag coi (รอบประมวลผล) | true |  |  |

## Exception

<อธิบายว่า มี exception อะไรที่ต้องจัดการหรือระวังบ้าง>

## Example Input & Output

1. <ตัวอย่างที่ 1 เช่น การส่งข้อมูลแบบปกติ>

```
<ตัวอย่าง data เช่น รูปแบบของ SOAP message>
```

```
<ตัวอย่าง response data เช่น รูปแบบของ SOAP message>
```
